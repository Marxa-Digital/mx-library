{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsDialog",
  "title": "Mx Dialog Schema",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the dialog.",
      "type": "string"
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the dialog.",
      "visible": false
    },
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    }
   },
  "required": [
    "name"
  ]
}
